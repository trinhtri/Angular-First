import { DateRangeDirective } from './date-range.directive';

describe('DateRangeDirective', () => {
  it('should create an instance', () => {
    // const directive = new DateRangeDirective();
    // expect(directive).toBeTruthy();
  });
});
